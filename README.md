## Công nghệ

- Go, Gin, GORM, PostgreSQL, Docker

## Cách chạy

### Chạy bằng Docker (chỉ cần cài Docker Desktop)

```bash
git clone https://github.com/nguyenhg2/restapi.git
cd restapi
docker-compose up --build
